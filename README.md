Locally:

To run the backend service:

cd /directory/robinhood-clone/user-service
mvn spring-boot:run



For Frontend:
cd /directory/robinhood-clone/frontend
npm run dev


Also need Postgres 15 on the system.
